pub enum PulseRegister {
    Volume,
    Sweep,
    Low,
    High
}

pub enum TriangleRegister {
	Linear,
	Low,
	High
}

pub enum NoiseRegister {
    Volume,
    Low,
    High
}

pub enum DMCRegister {
    Frequency,
    Raw,
    Start,
    Length
}